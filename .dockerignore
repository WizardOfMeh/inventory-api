# Secrets and local config — must never enter the build context
.env
.env.*
!.env.example
k8s/secret.yaml

# VCS and CI
.git
.github
.gitignore

# Build artifacts
bin/
dist/
*.test
coverage.out

# Docs and tooling
*.md
LICENSE
Makefile
Dockerfile
.dockerignore
docker-compose.yml
k8s/
